{
    "name": "mk8dx_twitch_bot",
    "description": "show some lounge stats from the current season of 150 cc mk8dx lounge in your twitch stream. (beta)",
    "env:" {
        "TWITCH_BOT_USERNAME": {
            "description": "Account name of twitch account",
            "required": true
        },
        "TWITCH_OAUTH_TOKEN" : {
            "description": "OAuth token of twitch account",
            "required": true
        },
        "TWITCH_SUPPORTED_CHANNELS": {
            "description": "Insert twitch channel name, where the bot should send the messages",
            "required": true
        }
    }
}